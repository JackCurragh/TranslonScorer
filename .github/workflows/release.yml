name: Release to PyPI

# Builds the sdist + wheel on every version tag and publishes to PyPI using
# Trusted Publishing (OIDC). No API token is stored in the repo.
#
# One-time setup on PyPI: add a Trusted Publisher for this repo/workflow under
# the TranslonScorer project settings
# (https://pypi.org/manage/project/translonscorer/), and create a GitHub
# environment named "pypi".

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build distributions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build

      - name: Check metadata
        run: |
          python -m pip install --upgrade twine
          twine check dist/*

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # required for Trusted Publishing
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1
