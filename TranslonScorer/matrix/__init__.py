"""Matrix subsystem: sparse cohort matrices — provider, p-site index, QC,
normalisation, frame-rollup, and profile clustering. Kept separate from the
streaming coverage providers in ``coverage/``."""
