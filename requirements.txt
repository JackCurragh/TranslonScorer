# Core Data Processing
# Use newer versions compatible with oxbow wheels (avoids building Rust)
polars==1.36.1
# Pin to NumPy <2 to avoid ABI mismatches with C-extensions (e.g., pyBigWig)
numpy>=1.26,<2
# Slightly older pandas pairs well with numpy 1.26 wheels in HPC envs
pandas==2.2.2
pyarrow==21.0.0
pysam==0.22.1
# Relax pyBigWig to allow solver to pick a build matching NumPy
pyBigWig>=0.3.22
pyfaidx==0.8.1.1
pyahocorasick==2.1.0
oxbow==0.5.1
pyranges==0.0.129
# Zarr v3 store compatibility (global_matrix.zarr uses v3 layout: zarr.json)
zarr>=3.1,<4
# Keep numcodecs <0.16 due to API removals; >=0.12 works with zarr v3
numcodecs>=0.12,<0.16

# CLI and Configuration
click>=8.1.7,<9
memory-profiler==0.61.0
# Ensure pkg_resources is available for PyRanges and friends
setuptools<70

# Visualization
matplotlib==3.9.0
plotly==5.22.0
Jinja2==3.1.4

# Development Dependencies (for testing)
pytest==8.2.0
